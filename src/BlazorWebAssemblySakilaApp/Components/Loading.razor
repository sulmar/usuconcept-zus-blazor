@typeparam TItem

@if (Items == null)
{
    <p>Loading items...</p>
}
else
{
    @ChildContent
}

@code {

    [Parameter]
    public List<TItem> Items  { get; set;  }

    [Parameter]
    public RenderFragment ChildContent { get; set; }

}
